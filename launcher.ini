[Settings]
; =================================================================
; Gold Monitor Launcher Configuration
; =================================================================
;
; python_path:
;   If set, the launcher will use this specific Python interpreter.
;   If left empty (default), the launcher will auto-detect in this order:
;     1. Local 'venv' folder
;     2. Local '.venv' folder
;     3. System 'python' command
;
; Example:
; python_path=D:\anaconda3\envs\my_env\python.exe
;
python_path=
